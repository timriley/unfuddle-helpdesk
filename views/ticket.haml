- @title = @ticket.ticket_summary

#ticket
  %h3= h(@ticket.ticket_summary)

  %table.tickets
    %thead
      %tr
        %th No.
        %th Summary
        %th Reporter
        %th Assignee
        %th Updated
    %tbody
      %tr{:class => "odd#{' unassigned' if !@ticket.assigned?}"}
        = partial('ticket_row', :locals => {:ticket => @ticket})
  
  %h4 Description
  
  %p.description
    = convert_breaks(h(@ticket.description))
    
  %h4 Comments
  
  - @ticket.comments.each_with_index do |comment, index|
    .comment
      .header
        %span.name
          = h(comment.commenter_name)
        %span.date
          = relative_time(comment.comment_created_at)
      .body
        %p
          = convert_breaks(h(comment.body))
      

  